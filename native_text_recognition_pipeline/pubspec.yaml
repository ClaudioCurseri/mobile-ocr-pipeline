name: text_recognition_pipeline
description: "Project containing a native bridge to a text recognition pipeline implemented in C++."
version: 0.0.1

environment:
  sdk: ^3.10.7

dependencies:
  code_assets: ^0.19.7
  flutter:
    sdk: flutter
  hooks: ^0.20.1
  logging: ^1.3.0
  native_assets_cli: ^0.18.0
  native_toolchain_c: ^0.17.1
  path_provider: ^2.1.5

dev_dependencies:
  ffi: ^2.1.3
  ffigen: ^13.0.0
  flutter_lints: ^6.0.0
  test: ^1.25.8
